{
  "name": "browser-use",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "build": "python -m pip install --upgrade pip setuptools wheel && python -m pip install -r requirements.txt && python setup.py sdist bdist_wheel"
  }
}
